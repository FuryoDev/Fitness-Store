import { ToCardinalPipe } from './to-cardinal.pipe';

describe('ToCardinalPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCardinalPipe();
    expect(pipe).toBeTruthy();
  });
});
